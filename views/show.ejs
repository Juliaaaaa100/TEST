<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do Details</title>
</head>
<body>

    <h1>To-Do Details</h1>
    <h2> <%= todo.title %> </h2>
    <p> Description: <%= todo.description %></p>
    <% if (todo.todoByDate) { %>
        <p>Due Date: <%= todo.todoByDate.toDateString() %></p>
    <% } else { %>
        <p>Due Date: No due date</p>
    <% } %>
    <a href="/todos/<%= todo._id %>/edit">Edit To-Do</a>
    <form action="/todos/<%= todo._id %>?_method=DELETE" method="POST">
        <input type="hidden" name="_method" value="DELETE"><br>
        <button type="submit">Delete To-Do</button><br><br>
    </form>
    <a href="/todos">Back to To-Do List</a>
</body>
</html>
